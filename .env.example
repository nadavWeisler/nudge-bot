BOT_TOKEN=your_bot_token_from_botfather

# Daily nudge time (24h format, server local time)
NUDGE_HOUR=8
NUDGE_MINUTE=0

# If set, only this group chat ID can use the bot (get it from /chatid)
ALLOWED_CHAT_ID=
